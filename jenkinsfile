pipeline {
    agent {
        node {
            label "build-1"
        }
    }
    
    stages{
        stage("Build") {
            steps {
                script{
                    mavenhome = tool "maven"
                    sh "${mavenhome}/bin/mvn -v"
                }
            }
        }
    }
}
